# Working source material

This directory contains editable or generated source material that is not
loaded by FilmViz at runtime:

- `profile_sources/` — SVG curve digitizations grouped by film stock;
- `source_artwork/` — editable artwork used during profile investigation;
- `rgb2spec/` — inputs and notes used to generate spectral tables.

Only reviewed runtime outputs belong under `Resources/`.
